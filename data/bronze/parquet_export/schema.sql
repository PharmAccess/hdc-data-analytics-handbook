CREATE SCHEMA gold;
CREATE SCHEMA silver;
CREATE SCHEMA bronze;



CREATE TABLE silver.patient_timeline(patient_id VARCHAR, social_security_number VARCHAR, prefix VARCHAR, first_name VARCHAR, last_name VARCHAR, birthDate DATE, code BIGINT, "system" VARCHAR, organization_id VARCHAR, organization_name VARCHAR, practitioner_name VARCHAR, practitioner_id VARCHAR, procedure_name VARCHAR, start_time VARCHAR, end_time VARCHAR, Vaccine_name VARCHAR, vaccine_code VARCHAR, vaccine_code_system VARCHAR);
CREATE TABLE silver.price_list(code VARCHAR, "system" VARCHAR, item_claimed VARCHAR, USD DOUBLE);
CREATE TABLE bronze.PractitionerRole(resourceType VARCHAR, id UUID, meta STRUCT(profile VARCHAR[]), practitioner STRUCT(identifier STRUCT("system" VARCHAR, "value" BIGINT), display VARCHAR), organization STRUCT(identifier STRUCT("system" VARCHAR, "value" UUID), display VARCHAR), code STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR)[], specialty STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR)[], "location" STRUCT(identifier STRUCT("system" VARCHAR, "value" UUID), display VARCHAR)[], telecom STRUCT("system" VARCHAR, "value" VARCHAR, "extension" STRUCT(url VARCHAR, valueBoolean BOOLEAN)[], "use" VARCHAR)[]);
CREATE TABLE bronze.Practitioner(resourceType VARCHAR, id UUID, meta STRUCT(profile VARCHAR[]), "extension" STRUCT(url VARCHAR, valueInteger BIGINT)[], identifier STRUCT("system" VARCHAR, "value" BIGINT)[], active BOOLEAN, "name" STRUCT("family" VARCHAR, given VARCHAR[], prefix VARCHAR[])[], telecom STRUCT("extension" STRUCT(url VARCHAR, valueBoolean BOOLEAN)[], "system" VARCHAR, "value" VARCHAR, "use" VARCHAR)[], address STRUCT(line VARCHAR[], city VARCHAR, state VARCHAR, postalCode VARCHAR, country VARCHAR)[], gender VARCHAR);
CREATE TABLE bronze.Immunization(resourceType VARCHAR, id UUID, meta STRUCT(profile VARCHAR[]), status VARCHAR, vaccineCode STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR), patient STRUCT(reference VARCHAR), encounter STRUCT(reference VARCHAR), occurrenceDateTime VARCHAR, primarySource BOOLEAN, "location" STRUCT(reference VARCHAR, display VARCHAR));
CREATE TABLE bronze."Location"(resourceType VARCHAR, id UUID, meta STRUCT(profile VARCHAR[]), identifier STRUCT("system" VARCHAR, "value" UUID)[], status VARCHAR, "name" VARCHAR, telecom STRUCT("system" VARCHAR, "value" VARCHAR)[], address STRUCT(line VARCHAR[], city VARCHAR, state VARCHAR, postalCode VARCHAR, country VARCHAR), "position" STRUCT(longitude DOUBLE, latitude DOUBLE), managingOrganization STRUCT(identifier STRUCT("system" VARCHAR, "value" UUID), display VARCHAR), description VARCHAR, "mode" VARCHAR, physicalType STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[]));
CREATE TABLE bronze.Organization(resourceType VARCHAR, id UUID, meta STRUCT(profile VARCHAR[]), "extension" STRUCT(url VARCHAR, valueInteger BIGINT)[], identifier STRUCT("system" VARCHAR, "value" UUID)[], active BOOLEAN, "type" STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR)[], "name" VARCHAR, telecom STRUCT("system" VARCHAR, "value" VARCHAR)[], address STRUCT(line VARCHAR[], city VARCHAR, state VARCHAR, postalCode VARCHAR, country VARCHAR)[]);
CREATE TABLE bronze.Patient(resourceType VARCHAR, id UUID, meta STRUCT(profile VARCHAR[]), "text" STRUCT(status VARCHAR, div VARCHAR), "extension" STRUCT(url VARCHAR, "extension" STRUCT(url VARCHAR, valueCoding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR), valueString VARCHAR)[], valueString VARCHAR, valueCode VARCHAR, valueAddress STRUCT(city VARCHAR, state VARCHAR, country VARCHAR), valueDecimal DOUBLE)[], identifier STRUCT("system" VARCHAR, "value" VARCHAR, "type" STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR))[], "name" STRUCT("use" VARCHAR, "family" VARCHAR, given VARCHAR[], prefix VARCHAR[], suffix VARCHAR[])[], telecom STRUCT("system" VARCHAR, "value" VARCHAR, "use" VARCHAR)[], gender VARCHAR, birthDate DATE, address STRUCT("extension" STRUCT(url VARCHAR, "extension" STRUCT(url VARCHAR, valueDecimal DOUBLE)[])[], line VARCHAR[], city VARCHAR, state VARCHAR, postalCode VARCHAR, country VARCHAR)[], maritalStatus STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR), multipleBirthBoolean BOOLEAN, communication STRUCT("language" STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR))[], multipleBirthInteger BIGINT);
CREATE TABLE bronze.Encounter(resourceType VARCHAR, id UUID, meta STRUCT(profile VARCHAR[]), identifier STRUCT("use" VARCHAR, "system" VARCHAR, "value" UUID)[], status VARCHAR, "class" STRUCT("system" VARCHAR, code VARCHAR), "type" STRUCT(coding STRUCT("system" VARCHAR, code BIGINT, display VARCHAR)[], "text" VARCHAR)[], subject STRUCT(reference VARCHAR, display VARCHAR), participant STRUCT("type" STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR)[], period STRUCT("start" VARCHAR, "end" VARCHAR), individual STRUCT(reference VARCHAR, display VARCHAR))[], period STRUCT("start" VARCHAR, "end" VARCHAR), "location" STRUCT("location" STRUCT(reference VARCHAR, display VARCHAR))[], serviceProvider STRUCT(reference VARCHAR, display VARCHAR));
CREATE TABLE bronze.DocumentReference(resourceType VARCHAR, id UUID, meta STRUCT(profile VARCHAR[]), identifier STRUCT("system" VARCHAR, "value" VARCHAR)[], status VARCHAR, "type" STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[]), category STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[])[], subject STRUCT(reference VARCHAR), date VARCHAR, author STRUCT(reference VARCHAR, display VARCHAR)[], custodian STRUCT(reference VARCHAR, display VARCHAR), "content" STRUCT(attachment STRUCT(contentType VARCHAR, "data" VARCHAR), format STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR))[], context STRUCT(encounter STRUCT(reference VARCHAR)[], period STRUCT("start" VARCHAR, "end" VARCHAR)));
CREATE TABLE bronze.ExplanationOfBenefit(resourceType VARCHAR, id UUID, contained STRUCT(resourceType VARCHAR, id VARCHAR, status VARCHAR, intent VARCHAR, subject STRUCT(reference VARCHAR), requester STRUCT(reference VARCHAR), performer STRUCT(reference VARCHAR)[], "type" STRUCT("text" VARCHAR), beneficiary STRUCT(reference VARCHAR), payor STRUCT(display VARCHAR)[])[], identifier STRUCT("system" VARCHAR, "value" VARCHAR)[], status VARCHAR, "type" STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR)[]), "use" VARCHAR, patient STRUCT(reference VARCHAR), billablePeriod STRUCT("start" VARCHAR, "end" VARCHAR), created VARCHAR, insurer STRUCT(display VARCHAR), provider STRUCT(reference VARCHAR), referral STRUCT(reference VARCHAR), facility STRUCT(reference VARCHAR, display VARCHAR), claim STRUCT(reference VARCHAR), outcome VARCHAR, careTeam STRUCT("sequence" BIGINT, provider STRUCT(reference VARCHAR), "role" STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[]))[], insurance STRUCT(focal BOOLEAN, coverage STRUCT(reference VARCHAR, display VARCHAR))[], item STRUCT("sequence" BIGINT, category STRUCT(coding STRUCT("system" VARCHAR, code BIGINT, display VARCHAR)[]), productOrService STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR), servicedPeriod STRUCT("start" VARCHAR, "end" VARCHAR), locationCodeableConcept STRUCT(coding STRUCT("system" VARCHAR, code BIGINT, display VARCHAR)[]), encounter STRUCT(reference VARCHAR)[], informationSequence BIGINT[], net STRUCT("value" DOUBLE, currency VARCHAR), adjudication STRUCT(category STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[]), amount STRUCT("value" DOUBLE, currency VARCHAR))[])[], total STRUCT(category STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR), amount STRUCT("value" DOUBLE, currency VARCHAR))[], payment STRUCT(amount STRUCT("value" DOUBLE, currency VARCHAR)));
CREATE TABLE bronze.Provenance(resourceType VARCHAR, id UUID, meta STRUCT(profile VARCHAR[]), target STRUCT(reference VARCHAR)[], recorded VARCHAR, agent STRUCT("type" STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR), who STRUCT(reference VARCHAR, display VARCHAR), onBehalfOf STRUCT(reference VARCHAR, display VARCHAR))[]);
CREATE TABLE bronze.Claim(resourceType VARCHAR, id UUID, status VARCHAR, "type" STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR)[]), "use" VARCHAR, patient STRUCT(reference VARCHAR, display VARCHAR), billablePeriod STRUCT("start" VARCHAR, "end" VARCHAR), created VARCHAR, provider STRUCT(reference VARCHAR, display VARCHAR), priority STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR)[]), facility STRUCT(reference VARCHAR, display VARCHAR), supportingInfo STRUCT("sequence" BIGINT, category STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR)[]), valueReference STRUCT(reference VARCHAR))[], insurance STRUCT("sequence" BIGINT, focal BOOLEAN, coverage STRUCT(display VARCHAR))[], item STRUCT("sequence" BIGINT, productOrService STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[], "text" VARCHAR), encounter STRUCT(reference VARCHAR)[], informationSequence BIGINT[], net STRUCT("value" DOUBLE, currency VARCHAR))[], total STRUCT("value" DOUBLE, currency VARCHAR));
CREATE TABLE bronze.DiagnosticReport(resourceType VARCHAR, id UUID, meta STRUCT(profile VARCHAR[]), status VARCHAR, category STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[])[], code STRUCT(coding STRUCT("system" VARCHAR, code VARCHAR, display VARCHAR)[]), subject STRUCT(reference VARCHAR), encounter STRUCT(reference VARCHAR), effectiveDateTime VARCHAR, issued VARCHAR, performer STRUCT(reference VARCHAR, display VARCHAR)[], presentedForm STRUCT(contentType VARCHAR, "data" VARCHAR)[]);




